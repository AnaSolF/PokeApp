import { type } from "os";

export type pokemon = {
    name: string,
    id: string,
    imgSrc: string
};

export type pokemonDetails = {
    name: string,
    id: string,
    imgSrc: string,
    hp: number,
    attack: number,
    defense:number,
};